# Copy to .env and export, or set in your shell / orchestrator.
HTTP_ADDR=:8080
LOG_LEVEL=info
WORKER_COUNT=2
WORKER_QUEUE_SIZE=256
SHUTDOWN_TIMEOUT_SEC=15
READ_TIMEOUT_SEC=30
WRITE_TIMEOUT_SEC=60
IDLE_TIMEOUT_SEC=120
